import javax.swing.*;
public class Test {
    public static void main(String[] args) {
        JFrame window = new JFrame();
        window.setTitle("Mein erstes GUI");
        window.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
        window.setSize(1280, 900);
        window.setVisible(true);
    }
}
